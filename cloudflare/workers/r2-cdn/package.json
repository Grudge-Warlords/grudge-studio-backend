{
  "name": "grudge-r2-cdn",
  "version": "1.0.0",
  "private": true,
  "description": "Grudge Studio R2 CDN Worker — edge-serves grudgedata bucket",
  "scripts": {
    "dev":    "wrangler dev",
    "deploy": "wrangler deploy",
    "tail":   "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
