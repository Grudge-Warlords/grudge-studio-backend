{
  "name": "grudge-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "Windows ↔ Linux VPS bridge — backups, dumps, SHA verification, deploy orchestration",
  "main": "dist/index.js",
  "scripts": {
    "dev": "nodemon --watch src -e ts --exec ts-node src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.540.0",
    "express": "^4.18.2",
    "node-cron": "^3.0.3",
    "mysql2": "^3.9.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "@types/node-cron": "^3.0.11",
    "nodemon": "^3.0.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
